// import React, { createContext, useState } from 'react';

// export const FaceSwapContext = createContext();

// // This context is currently not in use but kept for future integration
// export const FaceSwapProvider = ({ children }) => {
//   const [targetImage, setTargetImage] = useState(null);
//   const [targetPageIndex, setTargetPageIndex] = useState(null);
//   const [swappedResult, setSwappedResult] = useState(null);

//   return (
//     <FaceSwapContext.Provider value={{ targetImage, setTargetImage, targetPageIndex, setTargetPageIndex, swappedResult, setSwappedResult }}>
//       {children}
//     </FaceSwapContext.Provider>
//   );
// };
